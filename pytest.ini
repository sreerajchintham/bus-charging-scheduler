[pytest]
testpaths = tests
pythonpath = .
# Note: third-party plugin autoload must be disabled before pytest starts (Conda/langsmith).
# Use: make test | bcs-pytest | scripts/test.sh | PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest
